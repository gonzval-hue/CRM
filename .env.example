# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration (MariaDB - bidflow_ai)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=''
DB_NAME=bidflow_ai

# hPanel Deployment
# En producción (hPanel), usar variables de entorno del panel
# HOST=0.0.0.0
# PORT=8080
